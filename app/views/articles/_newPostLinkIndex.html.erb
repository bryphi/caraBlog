<% if logged_in? %>
    <header class = "">
        <div class = ""> 
            <button class="btn btn-default">
                <%= link_to "Create New Post", new_article_path %>
            </button>
        </div>
    </header>
<% end %>